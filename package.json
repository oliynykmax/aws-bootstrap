{
  "name": "aws-bootstrap",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "npx pm2 start ./server.js --name hello_aws --log ../logs/app.log",
    "stop": "npx pm2 stop hello_aws",
    "build": "echo 'Building...'"
  },
  "dependencies": {
    "pm2": "^6.0.14"
  }
}
